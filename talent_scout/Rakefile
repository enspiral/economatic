require 'rspec/core/rake_task'

desc "Run specs"
RSpec::Core::RakeTask.new do |t|
end

desc "Setup this console interface"
task :setup do
  puts "nothing to setup"
end

desc "Test this console interface"
task :ci => [:spec] do
end
